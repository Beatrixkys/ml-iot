//https://pub.dev/packages/google_mlkit_face_detection
//display camera
//create a face model 
//return list of coordintes from face model
//use list of coordinates to interpret emotion

//refer package below for emotion recognition
//https://flutterawesome.com/a-flutter-app-face-detection-and-emotion/
import 'package:flutter/material.dart';

class FaceRecScreen extends StatelessWidget {
  const FaceRecScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
